## What changed

<!-- Brief description of what this PR does -->

## Why

<!-- What problem does this solve or what feature does it add? -->

## Testing

<!-- How did you verify this works? -->

## Checklist

- [ ] Build passes: `xcodegen generate && xcodebuild -scheme Utterd -destination 'platform=macOS' build`
- [ ] Tests pass: `xcodebuild -scheme Utterd -destination 'platform=macOS' test`
- [ ] No credentials, keys, or secrets included
